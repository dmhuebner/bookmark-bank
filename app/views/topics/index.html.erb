<header class="row jumbotron">
	<div class="col-sm-8 col-sm-offset-2">
		<h1 class="text-center topics-heading">Bookmark Topics</h1>
	</div>
	<div class="col-sm-2">
		<%= link_to "New Topic", new_topic_path, class: "btn btn-primary btn-lg new-topic-btn" %>
	</div>
</header>

<div class="row">
	<div class="col-sm-10 col-sm-offset-1">
		<% @topics.each do |t| %>
		<div class="row">
			<div class="col-sm-12">
					<h3 class="bookmark-topic-title"><%= link_to t.title, t %>&nbsp;&nbsp;<%= link_to "Add Bookmark", new_topic_bookmark_path(t), class: "btn btn-primary pull-right", style: "display: inline-block" %></h3>
				<ul>
					<% t.bookmarks.each do |b| %>
						<div class="control-btn-group pull-right">
							<%= render partial: 'likes/like', locals: {topic: b.topic, bookmark: b} %>&nbsp;
							<%= render partial: 'bookmarks/options', locals: {topic: t, bookmark: b} %>
						</div>
						<!-- Show hide buttons below for use with UI later -->
						<h4 class="topic-bookmark clearfix"><li><%= link_to b.name, topic_bookmark_path(b.topic, b)%> - <%= link_to b.url, b.url, target: "_blank", class: "full-url" %> <span class="show-hide-btns"><a href="#" class="btn btn-default btn-xs view-url-btn hide">View URL</a><a href="#" class="btn btn-default btn-xs view-url-btn hide">Hide URL</a></span><small class="text-muted"> - <%= b.description %></small></li></h4>
					<% end %>
				</ul>
			</div>
		</div>
		<hr />
		<% end %>
	</div>
</div>

<!-- <div class="row">
	<div class="col-sm-10 col-sm-offset-1">
		<hr />
		<% @topics.each do |t| %>
		<div class="row">
			<div class="col-sm-10">
				<h3><%= link_to t.title, t %></h3>
				<% t.bookmarks.each do |b| %>
					<div class="col-sm-1 pull-right">
						<%= link_to "Edit", edit_topic_bookmark_path(t, b), class: "btn btn-default btn-xs" %>
					</div> -->
					<!-- Show hide buttons below for use with UI later -->
					<!-- <h4 class="topic-bookmark"><%= render partial: 'likes/like', locals: {topic: b.topic, bookmark: b} %>&nbsp;<%= link_to b.name, topic_bookmark_path(b.topic, b) %> - <%= link_to b.url, b.url, target: "_blank", class: "full-url" %> <span class="show-hide-btns"><a href="#" class="btn btn-default btn-xs view-url-btn hide">View URL</a><a href="#" class="btn btn-default btn-xs view-url-btn hide">Hide URL</a></span><small class="text-muted"> - <%= b.description %></small></h4>
				<% end %>
			</div>
			<div class="col-sm-2">
				<%= link_to "Add Bookmark", new_topic_bookmark_path(t), class: "btn btn-primary", style: "display: inline-block" %> -->
				<!-- <%= link_to "Edit Topic", edit_topic_path(t), class: "btn btn-default btn-xs edit-topic-btn" %> -->
				<!-- <%= link_to "Delete", t, method: :delete, class: "btn btn-danger btn-xs delete-topic-btn", data: {confirm: "Are you sure you want to delete the topic, \"#{t.title}\"?"} %> -->
			<!-- </div>
		</div>
		<hr />
		<% end %>
	</div>
</div> -->
